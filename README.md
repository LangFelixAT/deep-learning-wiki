# deep-learning-wiki

## Web wiki preview

The canonical notes live in `vault/`. A Quartz-based web preview lives in `quartz/` and renders the vault directly.

```powershell
cd quartz
npm run serve:vault
```

Then open `http://localhost:8080`.
